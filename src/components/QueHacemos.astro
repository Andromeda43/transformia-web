---
---

<section id="servicios" class="relative py-20 overflow-hidden">
  <!-- Grid Background -->
  <div class="grid-background"></div>
  
  <!-- Elementos decorativos -->
  <div class="decorative-circle" style="--size: 350px; --left: 5%; --bottom: 10%; --color: rgba(176, 66, 255, 0.05);"></div>
  <div class="decorative-circle" style="--size: 400px; --right: 8%; --top: 15%; --color: rgba(66, 135, 255, 0.07);"></div>
  
  <div class="container relative z-10">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold mb-4 neon-text" style="font-family: var(--font-mono);">¿QUÉ HACEMOS?</h2>
      <div class="w-24 h-1 bg-neon-purple mx-auto glow-effect"></div>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      <!-- Card 1: Observamos -->
      <div class="service-card" data-color="purple">
        <div class="service-icon purple">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
          </svg>
        </div>
        <h3 class="service-title">Observamos</h3>
        <p class="service-text">Analizamos tus procesos y detectamos oportunidades de mejora a través de diagnósticos profundos.</p>
        <div class="service-beam"></div>
        <div class="service-hover-effect"></div>
      </div>
      
      <!-- Card 2: Diseñamos -->
      <div class="service-card" data-color="blue">
        <div class="service-icon blue">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
          </svg>
        </div>
        <h3 class="service-title">Diseñamos</h3>
        <p class="service-text">Creamos soluciones personalizadas para tus necesidades específicas con un enfoque centrado en resultados.</p>
        <div class="service-beam"></div>
        <div class="service-hover-effect"></div>
      </div>
      
      <!-- Card 3: Implementamos -->
      <div class="service-card" data-color="pink">
        <div class="service-icon pink">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
          </svg>
        </div>
        <h3 class="service-title">Implementamos</h3>
        <p class="service-text">Ejecutamos las soluciones con metodologías ágiles para obtener resultados rápidos y consistentes.</p>
        <div class="service-beam"></div>
        <div class="service-hover-effect"></div>
      </div>
      
      <!-- Card 4: Acompañamos -->
      <div class="service-card" data-color="yellow">
        <div class="service-icon yellow">
          <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
          </svg>
        </div>
        <h3 class="service-title">Acompañamos</h3>
        <p class="service-text">Te guiamos durante todo el proceso de transformación con soporte continuo y capacitación a tu equipo.</p>
        <div class="service-beam"></div>
        <div class="service-hover-effect"></div>
      </div>
    </div>
    
    <!-- Decoraciones de fondo -->
    <div class="circle-decoration" style="--size: 6rem; --left: 5%; --bottom: 10%;"></div>
    <div class="circle-decoration" style="--size: 8rem; --right: 8%; --top: 20%;"></div>
  </div>
</section>

<style>
  #servicios {
    background-color: var(--color-dark-blue);
    color: white;
  }
  
  .circle-decoration {
    position: absolute;
    width: var(--size);
    height: var(--size);
    border: 1px solid var(--neon-blue);
    border-radius: 50%;
    opacity: 0.2;
    top: var(--top, auto);
    left: var(--left, auto);
    right: var(--right, auto);
    bottom: var(--bottom, auto);
    animation: pulse 4s ease-in-out infinite;
  }
  
  .service-card {
    background: rgba(26, 20, 45, 0.5);
    backdrop-filter: blur(10px);
    border-radius: 8px;
    padding: 2rem;
    border: 1px solid rgba(176, 66, 255, 0.2);
    transition: all 0.4s ease;
    overflow: hidden;
    position: relative;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }
  
  .service-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 25px rgba(176, 66, 255, 0.2);
  }
  
  .service-card[data-color="purple"]:hover {
    border-color: var(--neon-purple);
  }
  
  .service-card[data-color="blue"]:hover {
    border-color: var(--neon-blue);
  }
  
  .service-card[data-color="pink"]:hover {
    border-color: var(--neon-pink);
  }
  
  .service-card[data-color="yellow"]:hover {
    border-color: var(--neon-yellow);
  }
  
  .service-icon {
    margin-bottom: 1.5rem;
    color: var(--neon-purple);
    filter: drop-shadow(0 0 8px rgba(176, 66, 255, 0.7));
    transition: all 0.3s ease;
  }
  
  .service-icon.blue {
    color: var(--neon-blue);
    filter: drop-shadow(0 0 8px rgba(66, 135, 255, 0.7));
  }
  
  .service-icon.pink {
    color: var(--neon-pink);
    filter: drop-shadow(0 0 8px rgba(255, 66, 176, 0.7));
  }
  
  .service-icon.yellow {
    color: var(--neon-yellow);
    filter: drop-shadow(0 0 8px rgba(232, 255, 66, 0.7));
  }
  
  .service-title {
    font-size: 1.5rem;
    font-weight: bold;
    margin-bottom: 0.75rem;
    font-family: var(--font-mono);
  }
  
  .service-text {
    font-family: var(--font-sans);
    margin-bottom: 1rem;
    line-height: 1.6;
  }
  
  .service-beam {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, 
      var(--neon-purple) 0%, 
      var(--neon-blue) 50%, 
      var(--neon-purple) 100%
    );
    opacity: 0;
    transition: opacity 0.3s ease;
  }
  
  .service-card:hover .service-beam {
    opacity: 1;
    animation: beam-pulse 2s linear infinite;
  }
  
  .service-hover-effect {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 50% 50%, 
      rgba(176, 66, 255, 0.1) 0%, 
      transparent 70%
    );
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: -1;
  }
  
  .service-card:hover .service-hover-effect {
    opacity: 1;
  }
  
  .service-card[data-color="blue"]:hover .service-hover-effect {
    background: radial-gradient(circle at 50% 50%, 
      rgba(66, 135, 255, 0.1) 0%, 
      transparent 70%
    );
  }
  
  .service-card[data-color="pink"]:hover .service-hover-effect {
    background: radial-gradient(circle at 50% 50%, 
      rgba(255, 66, 176, 0.1) 0%, 
      transparent 70%
    );
  }
  
  .service-card[data-color="yellow"]:hover .service-hover-effect {
    background: radial-gradient(circle at 50% 50%, 
      rgba(232, 255, 66, 0.1) 0%, 
      transparent 70%
    );
  }
  
  @keyframes beam-pulse {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
  
  /* Responsivo */
  @media (max-width: 768px) {
    .circle-decoration {
      display: none;
    }
  }
</style>

<script>
  // Animación al hacer hover sobre las tarjetas
  document.addEventListener('DOMContentLoaded', () => {
    const serviceCards = document.querySelectorAll('.service-card');
    
    serviceCards.forEach(card => {
      // Efecto de iluminación al hover
      card.addEventListener('mouseenter', () => {
        const color = card.getAttribute('data-color');
        const icon = card.querySelector('.service-icon');
        
        if (color === 'purple') {
          icon.style.transform = 'scale(1.1)';
          icon.style.filter = 'drop-shadow(0 0 12px rgba(176, 66, 255, 0.9))';
        } else if (color === 'blue') {
          icon.style.transform = 'scale(1.1)';
          icon.style.filter = 'drop-shadow(0 0 12px rgba(66, 135, 255, 0.9))';
        } else if (color === 'pink') {
          icon.style.transform = 'scale(1.1)';
          icon.style.filter = 'drop-shadow(0 0 12px rgba(255, 66, 176, 0.9))';
        } else if (color === 'yellow') {
          icon.style.transform = 'scale(1.1)';
          icon.style.filter = 'drop-shadow(0 0 12px rgba(232, 255, 66, 0.9))';
        }
      });
      
      card.addEventListener('mouseleave', () => {
        const icon = card.querySelector('.service-icon');
        icon.style.transform = 'scale(1)';
        
        const color = card.getAttribute('data-color');
        if (color === 'purple') {
          icon.style.filter = 'drop-shadow(0 0 8px rgba(176, 66, 255, 0.7))';
        } else if (color === 'blue') {
          icon.style.filter = 'drop-shadow(0 0 8px rgba(66, 135, 255, 0.7))';
        } else if (color === 'pink') {
          icon.style.filter = 'drop-shadow(0 0 8px rgba(255, 66, 176, 0.7))';
        } else if (color === 'yellow') {
          icon.style.filter = 'drop-shadow(0 0 8px rgba(232, 255, 66, 0.7))';
        }
      });
    });
  });
</script>