---
---

<section id="nosotros" class="nosotros-section">
  <!-- Fondo con patrón de grid estrictamente limitado a esta sección -->
  <div class="nosotros-grid-bg"></div>
  
  <div class="container nosotros-container">
    <div class="nosotros-header">
      <h2 class="nosotros-title"><span class="glitch-effect">¿QUIÉNES SOMOS?</span></h2>
      <div class="nosotros-line"></div>
    </div>
    
    <div class="nosotros-cards">
      <!-- Tarjeta 1 -->
      <div class="nosotros-card">
        <div class="card-glow"></div>
        <div class="card-content">
          <div class="card-icon card-icon-essence">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10" />
              <path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20" />
              <path d="M2 12h20" />
            </svg>
          </div>
          <h3 class="card-title">NUESTRA ESENCIA</h3>
          <p class="card-text">Transformia es el aliado tecnológico que traduce desafíos empresariales en oportunidades de crecimiento digital, con enfoque innovador en cada solución.</p>
        </div>
        <div class="card-border-effect"></div>
      </div>
      
      <!-- Tarjeta 2 -->
      <div class="nosotros-card">
        <div class="card-glow"></div>
        <div class="card-content">
          <div class="card-icon card-icon-vision">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z" />
              <path d="M12 2v1" />
              <path d="M12 21v1" />
            </svg>
          </div>
          <h3 class="card-title">NUESTRA VISIÓN</h3>
          <p class="card-text">Buscamos ser referentes en la transformación digital, creando soluciones que eliminen la barrera entre los desafíos complejos y las oportunidades de innovación.</p>
        </div>
        <div class="card-border-effect"></div>
      </div>
      
      <!-- Tarjeta 3 -->
      <div class="nosotros-card">
        <div class="card-glow"></div>
        <div class="card-content">
          <div class="card-icon card-icon-approch">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M2 12h5" />
              <path d="M17 12h5" />
              <path d="M12 2v5" />
              <path d="M12 17v5" />
              <path d="m19 5-7 7-7-7" />
            </svg>
          </div>
          <h3 class="card-title">NUESTRO ENFOQUE</h3>
          <p class="card-text">Cada proyecto es una oportunidad para redefinir la forma en que la tecnología puede transformar tu negocio, con metodologías ágiles y soluciones personalizadas.</p>
        </div>
        <div class="card-border-effect"></div>
      </div>
    </div>
    
    <!-- Cita inspiradora con diseño retrofuturista -->
    <div class="retro-terminal-container">
      <div class="retro-terminal">
        <!-- Barra superior del terminal -->
        <div class="terminal-header">
          <div class="terminal-buttons">
            <span></span>
            <span></span>
            <span></span>
          </div>
          <div class="terminal-title">INNOVACIÓN.SYS</div>
          <div class="terminal-led"></div>
        </div>
        
        <!-- Pantalla -->
        <div class="terminal-screen">
          <!-- Efecto CRT -->
          <div class="crt-overlay"></div>
          <div class="scanline"></div>
          <div class="flicker"></div>
          
          <!-- Contenido -->
          <div class="screen-content">
            <div class="terminal-line">
              <span class="terminal-prompt">></span>
              <span class="terminal-command">load innovation_quote.dat</span>
            </div>
            
            <div class="screen-divider"></div>
            
            <div class="quote-display">
              <div class="loading-bar">
                <div class="loading-progress"></div>
              </div>
              
              <h3 class="quote-title">PENSAMIENTO INNOVADOR:</h3>
              
              <div class="terminal-output">
                <p class="quote-text">"La innovación distingue a los líderes de los seguidores."</p>
                <p class="quote-author">— Steve Jobs</p>
              </div>
              
              <div class="circuit-decoration left"></div>
              <div class="circuit-decoration right"></div>
            </div>
            
            <div class="screen-divider"></div>
            
            <div class="terminal-line">
              <span class="terminal-prompt">></span>
              <span class="terminal-command">execute transformation_protocol</span>
              <span class="terminal-cursor"></span>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Valores en lugar de métricas -->
    <div class="nosotros-valores">
      <h3 class="valores-title">NUESTROS VALORES</h3>
      
      <div class="valores-container">
        <div class="valor-item">
          <div class="valor-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="22 12 16 12 14 15 10 15 8 12 2 12"></polyline>
              <path d="M5.45 5.11L2 12v6a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-6l-3.45-6.89A2 2 0 0 0 16.76 4H7.24a2 2 0 0 0-1.79 1.11z"></path>
            </svg>
          </div>
          <h4 class="valor-title">Innovación</h4>
          <p class="valor-text">Buscamos constantemente nuevas formas de resolver problemas y crear valor.</p>
        </div>
        
        <div class="valor-item">
          <div class="valor-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="18" y1="6" x2="6" y2="18"></line>
              <line x1="6" y1="6" x2="18" y2="18"></line>
            </svg>
          </div>
          <h4 class="valor-title">Disrupción</h4>
          <p class="valor-text">No tememos desafiar lo establecido para encontrar mejores soluciones.</p>
        </div>
        
        <div class="valor-item">
          <div class="valor-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
            </svg>
          </div>
          <h4 class="valor-title">Excelencia</h4>
          <p class="valor-text">Nos comprometemos con la calidad en cada línea de código y cada interacción.</p>
        </div>
        
        <div class="valor-item">
          <div class="valor-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
              <circle cx="9" cy="7" r="4"></circle>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
            </svg>
          </div>
          <h4 class="valor-title">Colaboración</h4>
          <p class="valor-text">Trabajamos junto a ti, como socio tecnológico, no como simple proveedor.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* REGLAS GENERALES - Todo selector comienza con .nosotros para evitar conflictos */
  .nosotros-section {
    position: relative;
    padding: 6rem 0;
    background-color: #0a0a14;
    color: white;
    overflow: hidden;
    z-index: 1;
    box-sizing: border-box;
  }
  
  /* Regla global para asegurar box-sizing correcto */
  .nosotros-section *,
  .nosotros-section *::before,
  .nosotros-section *::after {
    box-sizing: border-box;
  }
  
  .nosotros-container {
    position: relative;
    width: 95%;
    max-width: 1280px;
    margin: 0 auto;
    z-index: 2;
    box-sizing: border-box;
  }
  
  @media (max-width: 767px) {
    .nosotros-container {
      width: 92%;
      padding: 0 10px;
    }
  }
  
  /* FONDO CON GRID - Estrictamente limitado a esta sección */
  .nosotros-grid-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
      linear-gradient(to right, rgba(128, 90, 213, 0.1) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(128, 90, 213, 0.1) 1px, transparent 1px);
    background-size: 40px 40px;
    z-index: 0;
    opacity: 0.4;
  }
  
  /* ENCABEZADO */
  .nosotros-header {
    text-align: center;
    margin-bottom: 4rem;
  }
  
  .nosotros-title {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: white;
    font-family: var(--font-title);
    position: relative;
    display: inline-block;
  }
  
  .nosotros-line {
    width: 120px;
    height: 3px;
    background: linear-gradient(90deg, 
      transparent, 
      var(--neon-purple) 30%, 
      var(--neon-purple) 70%, 
      transparent 100%
    );
    margin: 1rem auto 0;
    position: relative;
  }
  
  .nosotros-line::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 8px;
    height: 8px;
    background-color: var(--neon-purple);
    border-radius: 50%;
    box-shadow: 0 0 10px var(--neon-purple);
  }
  
  /* Efecto glitch sutil */
  .glitch-effect {
    position: relative;
    display: inline-block;
  }
  
  .glitch-effect::before,
  .glitch-effect::after {
    content: "¿QUIÉNES SOMOS?";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0.8;
  }
  
  .glitch-effect::before {
    color: #ff00ff;
    z-index: -1;
    animation: glitch-anim-1 3s infinite linear alternate;
  }
  
  .glitch-effect::after {
    color: #00ffff;
    z-index: -2;
    animation: glitch-anim-2 2s infinite linear alternate;
  }
  
  @keyframes glitch-anim-1 {
    0%, 100% { opacity: 0; transform: translateX(0); }
    5%, 30% { opacity: 0; transform: translateX(0); }
    31% { opacity: 0.3; transform: translateX(2px); }
    32% { opacity: 0; transform: translateX(0); }
    98% { opacity: 0; transform: translateX(0); }
    99% { opacity: 0.3; transform: translateX(-2px); }
  }
  
  @keyframes glitch-anim-2 {
    0%, 100% { opacity: 0; transform: translateX(0); }
    10%, 40% { opacity: 0; transform: translateX(0); }
    41% { opacity: 0.3; transform: translateX(-2px); }
    42% { opacity: 0; transform: translateX(0); }
    78% { opacity: 0; transform: translateX(0); }
    79% { opacity: 0.3; transform: translateX(2px); }
    80% { opacity: 0; transform: translateX(0); }
  }
  
  /* TARJETAS - Anchos ajustados para coincidir con la imagen de referencia */
  .nosotros-cards {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 4rem;
    width: 100%;
  }
  
  .nosotros-card {
    position: relative;
    background: rgba(26, 20, 45, 0.5);
    border-radius: 10px;
    padding: 2rem 1.5rem;
    overflow: hidden;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    z-index: 1;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    border: 1px solid rgba(176, 66, 255, 0.3);
    flex: 1;
    min-width: 0; /* Evita desbordamiento en flexbox */
  }
  
  /* Media queries ajustados para el espaciado horizontal */
  @media (max-width: 991px) {
    .nosotros-cards {
      flex-direction: column;
      gap: 30px;
    }
    
    .nosotros-card {
      width: 100%;
    }
  }
  
  @media (min-width: 768px) and (max-width: 991px) {
    .nosotros-container {
      width: 85%;
    }
    
    .nosotros-cards {
      flex-direction: row;
      flex-wrap: wrap;
    }
    
    .nosotros-card {
      flex: 0 0 calc(50% - 15px);
      max-width: calc(50% - 15px);
    }
  }
  
  @media (min-width: 992px) {
    .nosotros-container {
      width: 90%;
      max-width: 1280px; /* Aumentado para permitir tarjetas más anchas */
    }
    
    .nosotros-card {
      flex: 0 0 calc(33.333% - 15px);
      max-width: calc(33.333% - 15px);
    }
  }
  
  .nosotros-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3), 0 0 20px rgba(176, 66, 255, 0.2);
    border-color: rgba(176, 66, 255, 0.6);
  }
  
  .card-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 50% 50%, rgba(176, 66, 255, 0.1) 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: -1;
  }
  
  .nosotros-card:hover .card-glow {
    opacity: 1;
  }
  
  .card-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    height: 100%;
    width: 100%;
  }
  
  /* Iconos animados con tecnología */
  .card-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 70px;
    height: 70px;
    border-radius: 50%;
    margin-bottom: 1.5rem;
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
  }
  
  .card-icon-essence {
    background: rgba(176, 66, 255, 0.1);
    color: var(--neon-purple);
    box-shadow: 0 0 20px rgba(176, 66, 255, 0.2);
  }
  
  .card-icon-vision {
    background: rgba(66, 135, 255, 0.1);
    color: var(--neon-blue);
    box-shadow: 0 0 20px rgba(66, 135, 255, 0.2);
  }
  
  .card-icon-approch {
    background: rgba(255, 66, 176, 0.1);
    color: var(--neon-pink);
    box-shadow: 0 0 20px rgba(255, 66, 176, 0.2);
  }
  
  /* Animación de círculos concéntricos para los iconos */
  .card-icon::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 100%;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    border: 1px solid currentColor;
    opacity: 0;
    animation: icon-pulse 2s infinite;
  }
  
  .card-icon::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 100%;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    border: 1px solid currentColor;
    opacity: 0;
    animation: icon-pulse 2s infinite;
    animation-delay: 0.5s;
  }
  
  @keyframes icon-pulse {
    0% {
      width: 40%;
      height: 40%;
      opacity: 0.8;
    }
    100% {
      width: 140%;
      height: 140%;
      opacity: 0;
    }
  }
  
  .nosotros-card:hover .card-icon {
    transform: scale(1.1);
  }
  
  .nosotros-card:hover .card-icon-essence {
    box-shadow: 0 0 25px rgba(176, 66, 255, 0.4);
  }
  
  .nosotros-card:hover .card-icon-vision {
    box-shadow: 0 0 25px rgba(66, 135, 255, 0.4);
  }
  
  .nosotros-card:hover .card-icon-approch {
    box-shadow: 0 0 25px rgba(255, 66, 176, 0.4);
  }
  
  /* Animación de circuito digital en el fondo de las tarjetas */
  .nosotros-card::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke='%23b042ff' stroke-width='0.5' fill='none' d='M15,15 L85,15 L85,85 L15,85 Z M30,15 L30,30 L15,30 M55,15 L55,30 L70,30 L70,15 M85,45 L70,45 L70,70 L45,70 L45,85 M15,60 L30,60 L30,85'/%3E%3C/svg%3E");
    background-size: 150px 150px;
    background-position: center;
    opacity: 0.05;
    z-index: -1;
    transition: opacity 0.3s ease;
  }
  
  .nosotros-card:hover::after {
    opacity: 0.12;
  }
  
  .card-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1.2rem;
    color: white;
    font-family: var(--font-title);
    letter-spacing: 1px;
  }
  
  .card-text {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.6;
    flex-grow: 1;
    font-family: var(--font-body);
  }
  
  .card-border-effect {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--neon-purple), var(--neon-blue));
    transition: width 0.4s ease;
    box-shadow: 0 0 10px rgba(176, 66, 255, 0.5);
  }
  
  .nosotros-card:hover .card-border-effect {
    width: 100%;
  }
  
  /* CITA - Diseño retrofuturista estilo terminal */
  .retro-terminal-container {
    position: relative;
    max-width: 760px;
    margin: 5rem auto;
    perspective: 1000px;
    transform-style: preserve-3d;
  }
  
  .retro-terminal {
    position: relative;
    background: rgb(20, 20, 30);
    border-radius: 8px;
    border: 2px solid var(--neon-purple);
    box-shadow: 
      0 0 0 1px rgba(176, 66, 255, 0.3),
      0 0 15px rgba(176, 66, 255, 0.5),
      0 0 30px rgba(176, 66, 255, 0.2),
      inset 0 0 30px rgba(0, 0, 0, 0.7);
    overflow: hidden;
    z-index: 5;
  }
  
  /* Barra superior del terminal */
  .terminal-header {
    display: flex;
    align-items: center;
    background: linear-gradient(90deg, #1a142d, #252040);
    padding: 0.7rem 1rem;
    border-bottom: 2px solid rgba(176, 66, 255, 0.7);
  }
  
  .terminal-buttons {
    display: flex;
    gap: 8px;
    margin-right: 1rem;
  }
  
  .terminal-buttons span {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.4);
  }
  
  .terminal-buttons span:first-child {
    background: rgba(255, 80, 80, 0.7);
  }
  
  .terminal-buttons span:nth-child(2) {
    background: rgba(255, 180, 80, 0.7);
  }
  
  .terminal-buttons span:last-child {
    background: rgba(80, 200, 80, 0.7);
  }
  
  .terminal-title {
    flex-grow: 1;
    text-align: center;
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.8);
    letter-spacing: 2px;
    text-transform: uppercase;
  }
  
  .terminal-led {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--neon-purple);
    box-shadow: 0 0 8px var(--neon-purple);
    animation: led-blink 2s infinite alternate;
  }
  
  @keyframes led-blink {
    0%, 80% {
      opacity: 1;
    }
    90% {
      opacity: 0.3;
    }
    100% {
      opacity: 1;
    }
  }
  
  /* Pantalla del terminal */
  .terminal-screen {
    position: relative;
    background: #0a0a12;
    min-height: 320px;
    padding: 1.5rem;
    overflow: hidden;
  }
  
  /* Efectos CRT */
  .crt-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
      radial-gradient(ellipse at center, transparent 0%, rgba(10, 10, 18, 0.3) 90%), 
      linear-gradient(rgba(10, 10, 18, 0.1) 50%, rgba(0, 0, 0, 0.3) 50%);
    background-size: 100% 100%, 100% 2px;
    pointer-events: none;
    z-index: 3;
  }
  
  .scanline {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to bottom, 
      transparent 0%, 
      rgba(176, 66, 255, 0.1) 10%, 
      transparent 100%);
    opacity: 0.7;
    animation: scanline 8s linear infinite;
    pointer-events: none;
    z-index: 2;
  }
  
  @keyframes scanline {
    0% {
      transform: translateY(-100%);
    }
    100% {
      transform: translateY(100%);
    }
  }
  
  .flicker {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(176, 66, 255, 0.03);
    pointer-events: none;
    opacity: 0;
    z-index: 4;
    animation: flicker 0.2s infinite alternate;
  }
  
  @keyframes flicker {
    0%, 95% {
      opacity: 0;
    }
    100% {
      opacity: 0.2;
    }
  }
  
  /* Contenido de la pantalla */
  .screen-content {
    position: relative;
    color: #16e91d; /* Verde terminal clásico */
    font-family: var(--font-mono);
    z-index: 1;
  }
  
  .terminal-line {
    display: flex;
    align-items: center; /* Alineación vertical correcta */
    margin-bottom: 1rem;
    font-size: 0.9rem;
  }
  
  .terminal-prompt {
    color: var(--neon-purple);
    margin-right: 0.5rem;
  }
  
  .terminal-command {
    color: rgba(255, 255, 255, 0.9);
  }
  
  .terminal-cursor {
    position: relative;
    display: inline-block;
    width: 8px;
    height: 1em; /* Corregido: usa em en lugar de rem */
    background: var(--neon-purple);
    margin-left: 0.5rem;
    vertical-align: middle; /* Asegura alineación vertical correcta */
    animation: cursor-blink 1s infinite;
  }
  
  @keyframes cursor-blink {
    0%, 49% {
      opacity: 1;
    }
    50%, 100% {
      opacity: 0;
    }
  }
  
  .screen-divider {
    height: 1px;
    background: linear-gradient(90deg, 
      transparent 0%, 
      var(--neon-purple) 20%, 
      rgba(255, 255, 255, 0.8) 50%,
      var(--neon-purple) 80%, 
      transparent 100%);
    margin: 1rem 0;
    opacity: 0.4;
  }
  
  /* Bloque de visualización de la cita */
  .quote-display {
    position: relative;
    background: rgba(20, 20, 40, 0.3);
    border: 1px solid rgba(176, 66, 255, 0.4);
    border-radius: 4px;
    padding: 1.5rem;
    margin: 1.5rem 0;
    text-align: center;
  }
  
  .loading-bar {
    height: 4px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 2px;
    margin-bottom: 1.5rem;
    overflow: hidden;
  }
  
  .loading-progress {
    height: 100%;
    width: 0;
    background: linear-gradient(90deg, 
      var(--neon-purple) 0%, 
      var(--neon-blue) 50%, 
      var(--neon-purple) 100%);
    animation: loading 2s ease-in-out forwards;
  }
  
  @keyframes loading {
    0% {
      width: 0;
    }
    50% {
      width: 60%;
    }
    80% {
      width: 80%;
    }
    100% {
      width: 100%;
    }
  }
  
  .quote-title {
    font-size: 1rem;
    text-align: center;
    color: var(--neon-blue);
    margin-bottom: 1.5rem;
    letter-spacing: 2px;
  }
  
  .terminal-output {
    position: relative;
    background: linear-gradient(rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.2));
    padding: 1.5rem;
    border-radius: 4px;
  }
  
  .quote-text {
    font-size: clamp(1.2rem, 3vw, 1.8rem);
    font-weight: 600;
    margin-bottom: 1.2rem;
    color: white;
    text-shadow: 0 0 5px rgba(176, 66, 255, 0.7);
    animation: text-fade-in 2s ease-in-out forwards;
    opacity: 0;
    transform: translateY(10px);
  }
  
  @keyframes text-fade-in {
    0% {
      opacity: 0;
      transform: translateY(10px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .quote-author {
    font-size: 1.1rem;
    color: var(--neon-purple);
    animation: author-fade-in 2s ease-in-out forwards;
    animation-delay: 1s;
    opacity: 0;
  }
  
  @keyframes author-fade-in {
    0% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }
  
  /* Decoraciones de circuito */
  .circuit-decoration {
    position: absolute;
    width: 80px;
    height: 80px;
    background-image: url("data:image/svg+xml,%3Csvg width='80' height='80' viewBox='0 0 80 80' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke='%23b042ff' stroke-width='1' fill='none' d='M10,10 L70,10 L70,70 L10,70 Z M10,40 L40,40 M40,40 L40,70 M40,40 L40,10 M40,40 L70,40 M20,10 L20,25 M60,10 L60,25 M20,70 L20,55 M60,70 L60,55'/%3E%3Ccircle cx='40' cy='40' r='3' fill='%23b042ff'/%3E%3Ccircle cx='20' cy='25' r='2' fill='%23b042ff'/%3E%3Ccircle cx='60' cy='25' r='2' fill='%23b042ff'/%3E%3Ccircle cx='20' cy='55' r='2' fill='%23b042ff'/%3E%3Ccircle cx='60' cy='55' r='2' fill='%23b042ff'/%3E%3C/svg%3E");
    opacity: 0.4;
    z-index: -1;
  }
  
  .circuit-decoration.left {
    top: 50%;
    left: -40px;
    transform: translateY(-50%);
  }
  
  .circuit-decoration.right {
    top: 50%;
    right: -40px;
    transform: translateY(-50%);
  }
  
  /* VALORES - En lugar de métricas */
  .nosotros-valores {
    margin-top: 5rem;
  }
  
  .valores-title {
    text-align: center;
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 3rem;
    color: white;
    font-family: var(--font-title);
    position: relative;
  }
  
  .valores-title::after {
    content: '';
    position: absolute;
    bottom: -1rem;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 2px;
    background: var(--neon-purple);
  }
  
  .valores-container {
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: 2.5rem;
  }
  
  @media (min-width: 576px) and (max-width: 767px) {
    .valores-container {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }
  }
  
  @media (min-width: 768px) {
    .valores-container {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  
  .valor-item {
    text-align: center;
    transition: transform 0.3s ease;
  }
  
  .valor-item:hover {
    transform: translateY(-10px);
  }
  
  .valor-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 60px;
    height: 60px;
    margin: 0 auto 1.5rem;
    border-radius: 50%;
    background: rgba(176, 66, 255, 0.1);
    color: var(--neon-purple);
    transition: all 0.3s ease;
  }
  
  .valor-item:hover .valor-icon {
    background: rgba(176, 66, 255, 0.2);
    box-shadow: 0 0 15px rgba(176, 66, 255, 0.3);
    transform: rotate(10deg);
  }
  
  .valor-title {
    font-size: 1.2rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: white;
    font-family: var(--font-title);
  }
  
  .valor-text {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.8);
    font-family: var(--font-body);
  }
  
  /* Ajustes responsivos adicionales para terminal */
  @media (max-width: 576px) {
    .retro-terminal-container {
      margin: 3rem auto;
    }
    
    .terminal-screen {
      padding: 1rem;
      min-height: 280px;
    }
    
    .terminal-line {
      font-size: 0.8rem;
    }
    
    .quote-text {
      font-size: 1rem;
    }
    
    .terminal-title {
      font-size: 0.8rem;
    }
    
    .valores-title {
      font-size: 1.75rem;
    }
    
    .nosotros-title {
      font-size: 2.5rem;
    }
    
    .nosotros-header {
      margin-bottom: 3rem;
    }
    
    .nosotros-section {
      padding: 4rem 0;
    }
  }
  
  @media (max-width: 480px) {
    .screen-content {
      font-size: 0.85rem;
    }
    
    .quote-display {
      padding: 1rem;
    }
    
    .terminal-output {
      padding: 1rem;
    }
    
    .circuit-decoration {
      width: 60px;
      height: 60px;
    }
    
    .circuit-decoration.left {
      left: -20px;
    }
    
    .circuit-decoration.right {
      right: -20px;
    }
  }
</style>