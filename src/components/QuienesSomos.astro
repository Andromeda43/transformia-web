---
---

<section id="nosotros" class="nosotros-section">
  <!-- Fondo con patrón de grid estrictamente limitado a esta sección -->
  <div class="nosotros-grid-bg"></div>
  
  <div class="container nosotros-container">
    <div class="nosotros-header">
      <h2 class="nosotros-title"><span class="glitch-effect">¿QUIÉNES SOMOS?</span></h2>
      <div class="nosotros-line"></div>
    </div>
    
    <div class="nosotros-cards">
      <!-- Tarjeta 1 -->
      <div class="nosotros-card">
        <div class="card-glow"></div>
        <div class="card-content">
          <div class="card-icon card-icon-essence">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10" />
              <path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20" />
              <path d="M2 12h20" />
            </svg>
          </div>
          <h3 class="card-title">NUESTRA ESENCIA</h3>
          <p class="card-text">Transformia es el aliado tecnológico que traduce desafíos empresariales en oportunidades de crecimiento digital, con enfoque innovador en cada solución.</p>
        </div>
        <div class="card-border-effect"></div>
      </div>
      
      <!-- Tarjeta 2 -->
      <div class="nosotros-card">
        <div class="card-glow"></div>
        <div class="card-content">
          <div class="card-icon card-icon-vision">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M8.5 14.5A2.5 2.5 0 0 0 11 12c0-1.38-.5-2-1-3-1.072-2.143-.224-4.054 2-6 .5 2.5 2 4.9 4 6.5 2 1.6 3 3.5 3 5.5a7 7 0 1 1-14 0c0-1.153.433-2.294 1-3a2.5 2.5 0 0 0 2.5 2.5z" />
              <path d="M12 2v1" />
              <path d="M12 21v1" />
            </svg>
          </div>
          <h3 class="card-title">NUESTRA VISIÓN</h3>
          <p class="card-text">Buscamos ser referentes en la transformación digital, creando soluciones que eliminen la barrera entre los desafíos complejos y las oportunidades de innovación.</p>
        </div>
        <div class="card-border-effect"></div>
      </div>
      
      <!-- Tarjeta 3 -->
      <div class="nosotros-card">
        <div class="card-glow"></div>
        <div class="card-content">
          <div class="card-icon card-icon-approch">
            <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M2 12h5" />
              <path d="M17 12h5" />
              <path d="M12 2v5" />
              <path d="M12 17v5" />
              <path d="m19 5-7 7-7-7" />
            </svg>
          </div>
          <h3 class="card-title">NUESTRO ENFOQUE</h3>
          <p class="card-text">Cada proyecto es una oportunidad para redefinir la forma en que la tecnología puede transformar tu negocio, con metodologías ágiles y soluciones personalizadas.</p>
        </div>
        <div class="card-border-effect"></div>
      </div>
    </div>
    
    <!-- Cita inspiradora con diseño futurista -->
    <div class="nosotros-quote">
      <div class="quote-content">
        <span class="quote-mark">"</span>
        <div class="corner corner-tl"></div>
        <div class="corner corner-tr"></div>
        <div class="corner corner-bl"></div>
        <div class="corner corner-br"></div>
        
        <div class="quote-particle"></div>
        <div class="quote-particle"></div>
        <div class="quote-particle"></div>
        
        <p class="quote-text">La innovación distingue a los líderes de los seguidores.</p>
        <p class="quote-author">— Steve Jobs</p>
      </div>
    </div>
    
    <!-- Valores en lugar de métricas -->
    <div class="nosotros-valores">
      <h3 class="valores-title">NUESTROS VALORES</h3>
      
      <div class="valores-container">
        <div class="valor-item">
          <div class="valor-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="22 12 16 12 14 15 10 15 8 12 2 12"></polyline>
              <path d="M5.45 5.11L2 12v6a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-6l-3.45-6.89A2 2 0 0 0 16.76 4H7.24a2 2 0 0 0-1.79 1.11z"></path>
            </svg>
          </div>
          <h4 class="valor-title">Innovación</h4>
          <p class="valor-text">Buscamos constantemente nuevas formas de resolver problemas y crear valor.</p>
        </div>
        
        <div class="valor-item">
          <div class="valor-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="18" y1="6" x2="6" y2="18"></line>
              <line x1="6" y1="6" x2="18" y2="18"></line>
            </svg>
          </div>
          <h4 class="valor-title">Disrupción</h4>
          <p class="valor-text">No tememos desafiar lo establecido para encontrar mejores soluciones.</p>
        </div>
        
        <div class="valor-item">
          <div class="valor-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
            </svg>
          </div>
          <h4 class="valor-title">Excelencia</h4>
          <p class="valor-text">Nos comprometemos con la calidad en cada línea de código y cada interacción.</p>
        </div>
        
        <div class="valor-item">
          <div class="valor-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
              <circle cx="9" cy="7" r="4"></circle>
              <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
              <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
            </svg>
          </div>
          <h4 class="valor-title">Colaboración</h4>
          <p class="valor-text">Trabajamos junto a ti, como socio tecnológico, no como simple proveedor.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* REGLAS GENERALES - Todo selector comienza con .nosotros para evitar conflictos */
  .nosotros-section {
    position: relative;
    padding: 6rem 0;
    background-color: #0a0a14;
    color: white;
    overflow: hidden;
    z-index: 1;
  }
  
  .nosotros-container {
    position: relative;
    width: 90%;
    max-width: 1280px;
    margin: 0 auto;
    z-index: 2;
  }
  
  /* FONDO CON GRID - Estrictamente limitado a esta sección */
  .nosotros-grid-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
      linear-gradient(to right, rgba(128, 90, 213, 0.1) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(128, 90, 213, 0.1) 1px, transparent 1px);
    background-size: 40px 40px;
    z-index: 0;
    opacity: 0.4;
  }
  
  /* ENCABEZADO */
  .nosotros-header {
    text-align: center;
    margin-bottom: 4rem;
  }
  
  .nosotros-title {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: white;
    font-family: var(--font-title);
    position: relative;
    display: inline-block;
  }
  
  .nosotros-line {
    width: 120px;
    height: 3px;
    background: linear-gradient(90deg, 
      transparent, 
      var(--neon-purple) 30%, 
      var(--neon-purple) 70%, 
      transparent 100%
    );
    margin: 1rem auto 0;
    position: relative;
  }
  
  .nosotros-line::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 8px;
    height: 8px;
    background-color: var(--neon-purple);
    border-radius: 50%;
    box-shadow: 0 0 10px var(--neon-purple);
  }
  
  /* Efecto glitch sutil */
  .glitch-effect {
    position: relative;
    display: inline-block;
  }
  
  .glitch-effect::before,
  .glitch-effect::after {
    content: "¿QUIÉNES SOMOS?";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0.8;
  }
  
  .glitch-effect::before {
    color: #ff00ff;
    z-index: -1;
    animation: glitch-anim-1 3s infinite linear alternate;
  }
  
  .glitch-effect::after {
    color: #00ffff;
    z-index: -2;
    animation: glitch-anim-2 2s infinite linear alternate;
  }
  
  @keyframes glitch-anim-1 {
    0%, 100% { opacity: 0; transform: translateX(0); }
    5%, 30% { opacity: 0; transform: translateX(0); }
    31% { opacity: 0.3; transform: translateX(2px); }
    32% { opacity: 0; transform: translateX(0); }
    98% { opacity: 0; transform: translateX(0); }
    99% { opacity: 0.3; transform: translateX(-2px); }
  }
  
  @keyframes glitch-anim-2 {
    0%, 100% { opacity: 0; transform: translateX(0); }
    10%, 40% { opacity: 0; transform: translateX(0); }
    41% { opacity: 0.3; transform: translateX(-2px); }
    42% { opacity: 0; transform: translateX(0); }
    78% { opacity: 0; transform: translateX(0); }
    79% { opacity: 0.3; transform: translateX(2px); }
    80% { opacity: 0; transform: translateX(0); }
  }
  
  /* TARJETAS */
  .nosotros-cards {
    display: grid;
    grid-template-columns: repeat(1, 1fr);
    gap: 2rem;
    margin-bottom: 4rem;
  }
  
  @media (min-width: 768px) {
    .nosotros-cards {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  
  .nosotros-card {
    position: relative;
    background: rgba(26, 20, 45, 0.5);
    border-radius: 10px;
    padding: 2.5rem 1.8rem;
    overflow: hidden;
    transition: all 0.3s ease;
    height: 100%;
    display: flex;
    flex-direction: column;
    z-index: 1;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    border: 1px solid rgba(176, 66, 255, 0.3);
  }
  
  .nosotros-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3), 0 0 20px rgba(176, 66, 255, 0.2);
    border-color: rgba(176, 66, 255, 0.6);
  }
  
  .card-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 50% 50%, rgba(176, 66, 255, 0.1) 0%, transparent 70%);
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: -1;
  }
  
  .nosotros-card:hover .card-glow {
    opacity: 1;
  }
  
  .card-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    height: 100%;
  }
  
  /* Iconos animados con tecnología */
  .card-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 80px;
    height: 80px;
    border-radius: 50%;
    margin-bottom: 1.8rem;
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
  }
  
  .card-icon-essence {
    background: rgba(176, 66, 255, 0.1);
    color: var(--neon-purple);
    box-shadow: 0 0 20px rgba(176, 66, 255, 0.2);
  }
  
  .card-icon-vision {
    background: rgba(66, 135, 255, 0.1);
    color: var(--neon-blue);
    box-shadow: 0 0 20px rgba(66, 135, 255, 0.2);
  }
  
  .card-icon-approch {
    background: rgba(255, 66, 176, 0.1);
    color: var(--neon-pink);
    box-shadow: 0 0 20px rgba(255, 66, 176, 0.2);
  }
  
  /* Animación de círculos concéntricos para los iconos */
  .card-icon::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 100%;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    border: 1px solid currentColor;
    opacity: 0;
    animation: icon-pulse 2s infinite;
  }
  
  .card-icon::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 100%;
    height: 100%;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    border: 1px solid currentColor;
    opacity: 0;
    animation: icon-pulse 2s infinite;
    animation-delay: 0.5s;
  }
  
  @keyframes icon-pulse {
    0% {
      width: 40%;
      height: 40%;
      opacity: 0.8;
    }
    100% {
      width: 140%;
      height: 140%;
      opacity: 0;
    }
  }
  
  .nosotros-card:hover .card-icon {
    transform: scale(1.1);
  }
  
  .nosotros-card:hover .card-icon-essence {
    box-shadow: 0 0 25px rgba(176, 66, 255, 0.4);
  }
  
  .nosotros-card:hover .card-icon-vision {
    box-shadow: 0 0 25px rgba(66, 135, 255, 0.4);
  }
  
  .nosotros-card:hover .card-icon-approch {
    box-shadow: 0 0 25px rgba(255, 66, 176, 0.4);
  }
  
  /* Animación de circuito digital en el fondo de las tarjetas */
  .nosotros-card::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke='%23b042ff' stroke-width='0.5' fill='none' d='M15,15 L85,15 L85,85 L15,85 Z M30,15 L30,30 L15,30 M55,15 L55,30 L70,30 L70,15 M85,45 L70,45 L70,70 L45,70 L45,85 M15,60 L30,60 L30,85'/%3E%3C/svg%3E");
    background-size: 150px 150px;
    background-position: center;
    opacity: 0.05;
    z-index: -1;
    transition: opacity 0.3s ease;
  }
  
  .nosotros-card:hover::after {
    opacity: 0.12;
  }
  
  .card-title {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1.2rem;
    color: white;
    font-family: var(--font-title);
    letter-spacing: 1px;
  }
  
  .card-text {
    font-size: 1.05rem;
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.6;
    flex-grow: 1;
    font-family: var(--font-body);
  }
  
  .card-border-effect {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--neon-purple), var(--neon-blue));
    transition: width 0.4s ease;
    box-shadow: 0 0 10px rgba(176, 66, 255, 0.5);
  }
  
  .nosotros-card:hover .card-border-effect {
    width: 100%;
  }
  
  /* CITA - Rediseñada con tema tecnológico */
  .nosotros-quote {
    position: relative;
    max-width: 800px;
    margin: 7rem auto;
    padding: 0;
    z-index: 5;
  }
  
  .quote-content {
    position: relative;
    width: 100%;
    padding: 3.5rem 2.5rem;
    text-align: center;
    background: rgba(15, 15, 30, 0.7);
    backdrop-filter: blur(10px);
    border-radius: 12px;
    border: 1px solid rgba(176, 66, 255, 0.4);
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3), 0 0 30px rgba(176, 66, 255, 0.2);
    overflow: hidden;
  }
  
  /* Fondo tecnológico animado */
  .quote-content::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: 
      radial-gradient(circle at 20% 30%, rgba(176, 66, 255, 0.15) 0%, transparent 50%),
      radial-gradient(circle at 80% 70%, rgba(66, 135, 255, 0.1) 0%, transparent 50%);
    z-index: -1;
  }
  
  /* Red de circuitos animados */
  .quote-content::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath stroke='%23b042ff' stroke-width='0.5' fill='none' d='M10,10 L90,10 L90,90 L10,90 Z M10,50 L50,50 L50,90 M90,50 L50,50 M50,50 L50,10 M70,10 L70,30 L90,30 M10,70 L30,70 L30,90'/%3E%3Ccircle cx='50' cy='50' r='3' fill='%23b042ff' opacity='0.5'/%3E%3Ccircle cx='70' cy='30' r='2' fill='%23b042ff' opacity='0.5'/%3E%3Ccircle cx='30' cy='70' r='2' fill='%23b042ff' opacity='0.5'/%3E%3C/svg%3E");
    background-size: 200px 200px;
    opacity: 0.05;
    z-index: -1;
    transition: all 5s linear;
  }
  
  .nosotros-quote:hover .quote-content::after {
    background-position: 30px 30px;
    opacity: 0.1;
  }
  
  /* Efecto de energía pulsante */
  .corner {
    position: absolute;
    width: 15px;
    height: 15px;
    z-index: 3;
  }
  
  .corner-tl {
    top: 10px;
    left: 10px;
    border-top: 2px solid var(--neon-purple);
    border-left: 2px solid var(--neon-purple);
  }
  
  .corner-tr {
    top: 10px;
    right: 10px;
    border-top: 2px solid var(--neon-purple);
    border-right: 2px solid var(--neon-purple);
  }
  
  .corner-bl {
    bottom: 10px;
    left: 10px;
    border-bottom: 2px solid var(--neon-purple);
    border-left: 2px solid var(--neon-purple);
  }
  
  .corner-br {
    bottom: 10px;
    right: 10px;
    border-bottom: 2px solid var(--neon-purple);
    border-right: 2px solid var(--neon-purple);
  }
  
  /* Línea de energía pulsante en el borde */
  @keyframes border-pulse {
    0%, 100% {
      box-shadow: 0 0 8px var(--neon-purple);
      opacity: 0.8;
    }
    50% {
      box-shadow: 0 0 15px var(--neon-purple);
      opacity: 1;
    }
  }
  
  .corner {
    animation: border-pulse 2s infinite alternate;
  }
  
  .corner-tr {
    animation-delay: 0.5s;
  }
  
  .corner-bl {
    animation-delay: 1s;
  }
  
  .corner-br {
    animation-delay: 1.5s;
  }
  
  /* Marca de cita futurista */
  .quote-mark {
    position: absolute;
    top: -30px;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    background: var(--color-dark);
    color: var(--neon-purple);
    border-radius: 50%;
    border: 2px solid var(--neon-purple);
    box-shadow: 0 0 15px rgba(176, 66, 255, 0.5);
    z-index: 10;
  }
  
  /* Partículas tecnológicas */
  .quote-particle {
    position: absolute;
    width: 4px;
    height: 4px;
    background: var(--neon-purple);
    border-radius: 50%;
    box-shadow: 0 0 5px var(--neon-purple);
    z-index: 2;
    animation: float-particle 15s infinite linear;
  }
  
  .quote-particle:nth-child(2) {
    top: 25%;
    left: 75%;
    width: 6px;
    height: 6px;
    background: var(--neon-blue);
    box-shadow: 0 0 5px var(--neon-blue);
    animation-duration: 25s;
  }
  
  .quote-particle:nth-child(3) {
    top: 65%;
    left: 15%;
    width: 5px;
    height: 5px;
    animation-duration: 20s;
    animation-delay: 5s;
  }
  
  @keyframes float-particle {
    0% {
      transform: translate(0, 0);
    }
    25% {
      transform: translate(-50px, 30px);
    }
    50% {
      transform: translate(0, 60px);
    }
    75% {
      transform: translate(50px, 30px);
    }
    100% {
      transform: translate(0, 0);
    }
  }
  
  /* Texto de la cita */
  .quote-text {
    position: relative;
    font-size: 2rem;
    font-weight: 600;
    margin-bottom: 2rem;
    color: white;
    font-family: var(--font-title);
    line-height: 1.4;
    z-index: 2;
  }
  
  /* Efecto de tecnología digital en el texto */
  .quote-text::before {
    content: 'La innovación distingue a los líderes de los seguidores.';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    color: rgba(176, 66, 255, 0.5);
    z-index: -1;
    animation: text-glitch 5s infinite;
    opacity: 0;
  }
  
  @keyframes text-glitch {
    0%, 100% {
      opacity: 0;
      transform: translateX(0);
    }
    2%, 5% {
      opacity: 0.3;
      transform: translateX(-2px);
    }
    3%, 6% {
      opacity: 0;
      transform: translateX(0);
    }
    50%, 55% {
      opacity: 0;
    }
    52%, 54% {
      opacity: 0.3;
      transform: translateX(2px);
    }
  }
  
  /* Autor de la cita */
  .quote-author {
    position: relative;
    font-size: 1.2rem;
    color: var(--neon-purple);
    font-family: var(--font-mono);
    font-weight: 700;
    z-index: 2;
    display: inline-block;
    padding: 0.5rem 1.5rem;
    background: rgba(176, 66, 255, 0.1);
    border-radius: 30px;
    box-shadow: 0 0 15px rgba(176, 66, 255, 0.2);
    border: 1px solid rgba(176, 66, 255, 0.3);
  }
  
  /* VALORES - En lugar de métricas */
  .nosotros-valores {
    margin-top: 5rem;
  }
  
  .valores-title {
    text-align: center;
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 3rem;
    color: white;
    font-family: var(--font-title);
    position: relative;
  }
  
  .valores-title::after {
    content: '';
    position: absolute;
    bottom: -1rem;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 2px;
    background: var(--neon-purple);
  }
  
  .valores-container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2.5rem;
  }
  
  @media (min-width: 768px) {
    .valores-container {
      grid-template-columns: repeat(4, 1fr);
    }
  }
  
  .valor-item {
    text-align: center;
    transition: transform 0.3s ease;
  }
  
  .valor-item:hover {
    transform: translateY(-10px);
  }
  
  .valor-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 60px;
    height: 60px;
    margin: 0 auto 1.5rem;
    border-radius: 50%;
    background: rgba(176, 66, 255, 0.1);
    color: var(--neon-purple);
    transition: all 0.3s ease;
  }
  
  .valor-item:hover .valor-icon {
    background: rgba(176, 66, 255, 0.2);
    box-shadow: 0 0 15px rgba(176, 66, 255, 0.3);
    transform: rotate(10deg);
  }
  
  .valor-title {
    font-size: 1.2rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: white;
    font-family: var(--font-title);
  }
  
  .valor-text {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.8);
    font-family: var(--font-body);
  }
</style>